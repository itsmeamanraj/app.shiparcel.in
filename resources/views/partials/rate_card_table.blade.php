<!-- <table class="table table-bordered">
    <thead>
        <tr>
            <th>Zone</th>
            <th>Forward Price</th>
            <th>Additional Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach($rates as $rate)
        <tr>
            <td>{{ $rate->zone }}</td>
            <td>{{ number_format($rate->forward_fwd, 2) }}</td>
            <td>{{ number_format($rate->additional_fwd, 2) }}</td>
        </tr>
        @endforeach
    </tbody>
</table> -->
